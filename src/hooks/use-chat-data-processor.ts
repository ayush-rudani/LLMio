/**
 * @deprecated This hook is no longer used in AI SDK v5.
 * Custom data parts (thread_id, stream_id) are now handled via the `onData` callback
 * in useChatIntegration. See use-chat-integration.ts for the implementation.
 *
 * Keeping this file for reference during migration.
 */

// The `data` property was removed from useChat in AI SDK v5.
// Instead, custom data parts are now handled via the `onData` callback in useChat.
// See: https://ai-sdk.dev/docs/ai-sdk-ui/streaming-data

export {}
